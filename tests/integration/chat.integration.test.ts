describe('Chat Integration', () => {
  it('POST /v1/chat/completions', () => {
    // uses mock HTTP server
  });

  it('streaming SSE parsed correctly', () => {
    // integration test
  });

  it('request headers validated', () => {
    // integration test
  });
});
