describe('Retry Logic', () => {
  it('retries on 500 (integration test)', () => {
    // covered in integration tests
  });

  it('does not retry on 401', () => {
    // covered in integration tests
  });

  it('exponential backoff respected', () => {
    // covered in integration tests
  });
});
